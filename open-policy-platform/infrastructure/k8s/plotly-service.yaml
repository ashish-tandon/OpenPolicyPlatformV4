apiVersion: apps/v1
kind: Deployment
metadata:
  name: plotly-service
  namespace: openpolicy
  labels:
    app: plotly-service
    tier: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: plotly-service
  template:
    metadata:
      labels:
        app: plotly-service
        tier: api
    spec:
      containers:
      - name: plotly-service
        image: plotly-service:latest
        ports:
        - containerPort: 9019
        env:
        - name: PORT
          value: "9019"
        - name: ENVIRONMENT
          value: "production"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 9019
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /readyz
            port: 9019
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: logs
          mountPath: /app/logs
      volumes:
      - name: logs
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: plotly-service
  namespace: openpolicy
  labels:
    app: plotly-service
    tier: api
spec:
  type: ClusterIP
  ports:
  - port: 9019
    targetPort: 9019
    protocol: TCP
    name: http
  selector:
    app: plotly-service
