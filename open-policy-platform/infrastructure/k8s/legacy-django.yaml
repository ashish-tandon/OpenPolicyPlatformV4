apiVersion: apps/v1
kind: Deployment
metadata:
  name: legacy-django
  namespace: openpolicy
  labels:
    app: legacy-django
    tier: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: legacy-django
  template:
    metadata:
      labels:
        app: legacy-django
        tier: api
    spec:
      containers:
      - name: legacy-django
        image: legacy-django:latest
        ports:
        - containerPort: 8010
        env:
        - name: PORT
          value: "8010"
        - name: ENVIRONMENT
          value: "production"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8010
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8010
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: logs
          mountPath: /app/logs
      volumes:
      - name: logs
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: legacy-django-service
  namespace: openpolicy
  labels:
    app: legacy-django
    tier: api
spec:
  type: ClusterIP
  ports:
  - port: 8010
    targetPort: 8010
    protocol: TCP
    name: http
  selector:
    app: legacy-django