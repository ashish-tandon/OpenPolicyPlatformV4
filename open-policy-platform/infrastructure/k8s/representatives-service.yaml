apiVersion: apps/v1
kind: Deployment
metadata:
  name: representatives-service
  namespace: openpolicy
  labels:
    app: representatives-service
    tier: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: representatives-service
  template:
    metadata:
      labels:
        app: representatives-service
        tier: api
    spec:
      containers:
      - name: representatives-service
        image: representatives-service:latest
        ports:
        - containerPort: 8014
        env:
        - name: PORT
          value: "8014"
        - name: ENVIRONMENT
          value: "production"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8014
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8014
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: logs
          mountPath: /app/logs
      volumes:
      - name: logs
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: representatives-service
  namespace: openpolicy
  labels:
    app: representatives-service
    tier: api
spec:
  type: ClusterIP
  ports:
  - port: 8014
    targetPort: 8014
    protocol: TCP
    name: http
  selector:
    app: representatives-service